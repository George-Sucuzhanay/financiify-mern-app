

export const PurchaseStock = (props) => {

  
  return (
    <div>
      <div>
        {/* <h1>{props.symbol.description}</h1> */}
        {/* <h1>{props.symbol.symbol}</h1>
        <h3>{props.price}</h3> */}
        
      </div>
    </div>
  );
};
